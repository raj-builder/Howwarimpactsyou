# ─── External API Keys ──────────────────────────────────────────
# EIA Open Data API — free registration at https://www.eia.gov/opendata/register.php
# Used by /api/fuel-digest to fetch Brent crude + jet fuel spot prices.
# Optional: the app works without it (falls back to static data).
EIA_API_KEY=your_eia_api_key_here

# ─── Existing keys (if any) ────────────────────────────────────
# SERPAPI_KEY=your_serpapi_key_here
